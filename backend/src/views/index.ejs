<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Inbox to Week</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50">
  <div class="min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full bg-white shadow-lg rounded-lg p-8">
      <h1 class="text-3xl font-bold text-gray-900 mb-4">Inbox to Week</h1>
      <p class="text-gray-600 mb-6">Healthcare emails and photos → One weekly plan</p>

      <% if (user) { %>
        <p class="text-sm text-gray-700">Welcome, <%= user.email %></p>
        <a href="/plan" class="mt-4 block w-full bg-blue-600 text-white text-center py-2 px-4 rounded hover:bg-blue-700">
          View Plan
        </a>
        <a href="/settings" class="mt-2 block w-full bg-gray-200 text-gray-800 text-center py-2 px-4 rounded hover:bg-gray-300">
          Settings
        </a>
        <a href="/auth/logout" class="mt-2 block w-full text-center text-sm text-gray-600 hover:text-gray-800">
          Sign Out
        </a>
      <% } else { %>
        <a href="/auth/google" class="block w-full bg-blue-600 text-white text-center py-3 px-4 rounded hover:bg-blue-700">
          Sign in with Google
        </a>
      <% } %>
    </div>
  </div>
</body>
</html>

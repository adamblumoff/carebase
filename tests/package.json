{
  "name": "@carebase/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "test": "NODE_ENV=test vitest run",
    "test:coverage": "NODE_ENV=test vitest run --coverage --coverage.reports=text && node -e \"const fs=require('fs');const p='coverage';if(fs.existsSync(p))fs.rmSync(p,{recursive:true,force:true});\""
  },
  "dependencies": {
    "pg-mem": "^3.0.5",
    "supertest": "^7.1.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.1",
    "typescript": "^5.9.3",
    "vitest": "^2.1.1"
  },
  "engines": {
    "node": ">=20.19.4"
  }
}
